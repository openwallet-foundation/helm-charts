apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-article.fullname" . }}
  labels:
    {{- include "test-article.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-article.selectorLabels" . | nindent 4 }}
