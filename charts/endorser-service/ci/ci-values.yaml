# CI test values - disable persistence and autoscaling for faster testing
autoscaling:
  enabled: false

# Kind may enforce NetworkPolicy; keep CI focused on chart install readiness.
networkPolicy:
  enabled: false

proxy:
  autoscaling:
    enabled: false

acapy:
  autoscaling:
    enabled: false
  persistence:
    enabled: false
  postgres:
    persistence:
      enabled: false

postgres:
  persistence:
    enabled: false
