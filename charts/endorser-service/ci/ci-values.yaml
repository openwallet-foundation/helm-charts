# CI test values - disable persistence and autoscaling for faster testing
autoscaling:
  enabled: false

proxy:
  autoscaling:
    enabled: false

acapy:
  autoscaling:
    enabled: false
  persistence:
    enabled: false
  postgres:
    persistence:
      enabled: false

postgres:
  persistence:
    enabled: false
